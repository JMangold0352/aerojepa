# Core runtime dependencies. These are enough to generate synthetic drone
# clips, train the model, evaluate it, and render the publication figures.
torch>=2.2.0
torchvision>=0.17.0
numpy>=1.26.0
pyyaml>=6.0
tqdm>=4.66.0
matplotlib>=3.8.0

# AeroProber side-project (research/prober/): ground-truth metric state for
# supervised prober training. PyBullet physics runs on CPU; model training
# stays on MPS, so there is no device conflict.
PyFlyt>=0.20.0
